import React from "react"
import ReactDOM from 'react-dom/client';

export default function Footer() {
    return (
        <div>
            <small>
                This is footer.
            </small>
        </div>
    )
}